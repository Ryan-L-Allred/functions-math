const addTwoNumbers = (add1, add2) => {
    return add1 + add2
}
const add = addTwoNumbers(7, 12)


const multiplyNumbers = (factor) => {
    return factor * 2
}
const multiply = multiplyNumbers(add)
console.log(multiply)

